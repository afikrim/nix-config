# .envrc for accounting_service
# Copy this file to .envrc
# Then run: direnv allow

use flake ~/nix-config#accounting_service

# Load local environment variables if they exist
dotenv_if_exists .env.local
