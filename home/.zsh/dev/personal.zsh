#!/usr/bin/env zsh

# Reserved for personal projects. Add helpers/aliases here if needed.
# For now we export shortcuts to quickly jump into top-level folders.

: "${PERSONAL_DEV_DIR:=$HOME/Development/personal}"

alias dev-mine='cd "$PERSONAL_DEV_DIR"'
